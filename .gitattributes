* text=auto

# Scripts que precisam de EOL específico
gradlew text eol=lf
*.sh text eol=lf
*.bat text eol=crlf

# Binários — nunca converter
*.jar binary
*.png binary
*.ico binary

# Markdown sempre LF na working tree (evita ruido de EOL entre Windows e CI)
*.md text eol=lf
